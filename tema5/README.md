# StudentPortfolio — Тема 5 (об'єднання)

Гілка `tema5` = `sr5` + `hw5` (git-merge: різні папки) + цілісний проєкт `tema5/`.
C++/CLI WinForms, .NET 4.8.

## Підсумок Теми 5
- Міграція console → WinForms; модель `Person/Student/Teacher/Staff` із tema4.
- Форма: введення даних студента, валідація (ім'я/стать/вік 16–100), `marshal_as`,
  вивід `getInfo()` у Label через `gcnew String`, поле **Pets**, кнопка **Clear**.

Наступний крок: `sr6/hw6/tema6` (взаємодія форм — StudentEditForm). Збірка Debug|x64 без помилок.
