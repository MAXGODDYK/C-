# StudentPortfolio — Домашнє завдання 5 (Тема 5)

Гілка `hw5` — від `tema4` (паралельно до `sr5`). C++/CLI WinForms, .NET 4.8.
Відкрити `StudentPortfolio.sln` (Debug | x64), `F5`.

## Виконано (ДЗ5) — маркери `// ДЗ5 #k` у `MainForm.h`
- **#1** Додано TextBox для індивідуального поля **Pets** (homePets), значення йде в `Student`.
- **#2** Повна валідація: ім'я/прізвище не порожні, стать обрана, вік 16–100.
- **#3** При помилці — `MessageBox::Show`.
- **#4** Реальні дані за замовчуванням.
- **Варіант Б**: кнопка **Clear** скидає всі поля та Label (`btnClear_Click`).

База СР5 (підписка Click, marshal_as, gcnew String) збережена.
Коментарі українською, код англійською. Збірка Debug|x64 без помилок.
